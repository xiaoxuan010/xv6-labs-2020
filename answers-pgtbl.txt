1. vmprint 的输出中，page 1 包含了可执行代码和数据；Page 2 是用户栈，用户可读写；page 1 是 guard page，用户模式下不可访问。
2. 第三个判断条件 srcva + len < srcva 是为了防止整数溢出带来的安全隐患。例如：srcva = UINT64_MAX - 10，len = 20，此时前两个判断条件都不成立，但 srcva + len 会溢出变成一个很小的数，第三个判断条件可以捕捉到这种情况，确保函数的安全性。
